//this file is generated by program(drogon_ctl) automatically,don't modify it!
#include "balance.h"
#include <drogon/utils/OStringStream.h>
#include <drogon/utils/Utilities.h>
#include <string>
#include <map>
#include <vector>
#include <set>
#include <iostream>
#include <unordered_map>
#include <unordered_set>
#include <algorithm>
#include <list>
#include <deque>
#include <queue>
using namespace drogon;
std::string balance::genText(const DrTemplateData& balance_view_data)
{
	drogon::OStringStream balance_tmp_stream;
	std::string layoutName{""};
	balance_tmp_stream << "<!DOCTYPE html>\n";
	balance_tmp_stream << "<html>\n";
	balance_tmp_stream << "<head>\n";
	balance_tmp_stream << "    <title>Balance</title>\n";
	balance_tmp_stream << "    <style>\n";
	balance_tmp_stream << "        .balance-value {\n";
	balance_tmp_stream << "            font-size: 2em;\n";
	balance_tmp_stream << "            color: green;\n";
	balance_tmp_stream << "        }\n";
	balance_tmp_stream << "        .go-back-container {\n";
	balance_tmp_stream << "            position: fixed;\n";
	balance_tmp_stream << "            top: 20px;\n";
	balance_tmp_stream << "            right: 20px;\n";
	balance_tmp_stream << "        }\n";
	balance_tmp_stream << "        .button {\n";
	balance_tmp_stream << "            padding: 10px 20px;\n";
	balance_tmp_stream << "            background-color: #4CAF50; /* Green background */\n";
	balance_tmp_stream << "            color: white;\n";
	balance_tmp_stream << "            border: none;\n";
	balance_tmp_stream << "            border-radius: 5px;\n";
	balance_tmp_stream << "            cursor: pointer;\n";
	balance_tmp_stream << "            font-size: 16px;\n";
	balance_tmp_stream << "        }\n";
	balance_tmp_stream << "        .button:hover {\n";
	balance_tmp_stream << "            background-color: #45a049; /* Slightly darker green on hover */\n";
	balance_tmp_stream << "        }\n";
	balance_tmp_stream << "    </style>\n";
	balance_tmp_stream << "</head>\n";
	balance_tmp_stream << "<body>\n";
	balance_tmp_stream << "    <h1>Your Current Balance</h1>\n";
	balance_tmp_stream << "    <p class=\"balance-value\">Balance: ";
{
    auto & val=balance_view_data["balance"];
    if(val.type()==typeid(const char *)){
        balance_tmp_stream<<*(std::any_cast<const char *>(&val));
    }else if(val.type()==typeid(std::string)||val.type()==typeid(const std::string)){
        balance_tmp_stream<<*(std::any_cast<const std::string>(&val));
    }
}
	balance_tmp_stream << " </p>\n";
balance_tmp_stream<<"\n";
	balance_tmp_stream << "    <form action=\"/balance/update\" method=\"POST\">\n";
	balance_tmp_stream << "        <input type=\"number\" name=\"newBalance\" step=\"0.01\" required placeholder=\"Enter new balance\">\n";
	balance_tmp_stream << "        <select name=\"operation\" required>\n";
	balance_tmp_stream << "            <option value=\"add\">Add</option>\n";
	balance_tmp_stream << "            <option value=\"subtract\">Subtract</option>\n";
	balance_tmp_stream << "        </select>\n";
	balance_tmp_stream << "        <button type=\"submit\">Update Balance</button>\n";
	balance_tmp_stream << "    </form>\n";
	balance_tmp_stream << "    <div class=\"go-back-container\">\n";
	balance_tmp_stream << "        <button class=\"button\" onclick=\"window.location.href='/';\">Go Back</button>\n";
	balance_tmp_stream << "    </div>\n";
	balance_tmp_stream << "</body>\n";
	balance_tmp_stream << "</html>\n";
if(layoutName.empty())
{
std::string ret{std::move(balance_tmp_stream.str())};
return ret;
}else
{
auto templ = DrTemplateBase::newTemplate(layoutName);
if(!templ) return "";
HttpViewData data = balance_view_data;
auto str = std::move(balance_tmp_stream.str());
if(!str.empty() && str[str.length()-1] == '\n') str.resize(str.length()-1);
data[""] = std::move(str);
return templ->genText(data);
}
}
